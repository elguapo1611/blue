description "start iptables"

task

start on filesystem <%= Blue::Box.current.all_interfaces.map{|interface| "and net-device-up IFACE=#{interface}"}.join(' ') %>

exec iptables-restore < /etc/iptables.rules

